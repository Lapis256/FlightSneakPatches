1. copy `docs/data` directory to `run/saves/<WorldPath>/datapacks` directory.
2. execute `/function flight_sneak_patches:get_summoning_staff` in Minecraft.
3. The main-hand item is replaced with Summoning Stuff.
